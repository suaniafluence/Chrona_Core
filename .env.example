DOMAIN=example.com
TRAEFIK_ACME_EMAIL=admin@example.com

# Backend security (enable only when HTTPS enforced)
ENABLE_HSTS=false

# Backend CORS
ALLOWED_ORIGINS=https://backoffice.${DOMAIN},https://kiosk.${DOMAIN}
ALLOW_CREDENTIALS=false
ALLOWED_METHODS=*
ALLOWED_HEADERS=*

# Optional: Database URL for local runs (Docker Compose sets its own)
# DATABASE_URL=postgresql+asyncpg://chrona:chrona@localhost:5432/chrona

