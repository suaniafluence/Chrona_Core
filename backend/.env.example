# Backend environment example

# Database connection (adjust to your setup)
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/chrona

# CORS origins (comma-separated) - includes backoffice and kiosk dev ports
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# App secrets and config
SECRET_KEY=change-me
LOG_LEVEL=info

# CORS behavior
# Accepts comma-separated values; use * to allow any
ALLOW_CREDENTIALS=false
ALLOWED_METHODS=*
ALLOWED_HEADERS=*

# ==================== Email Configuration ====================

# Email provider: smtp, sendgrid, or console (for dev)
EMAIL_PROVIDER=console

# SMTP Configuration (for Gmail, Outlook, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_USE_TLS=true

# SendGrid Configuration (alternative to SMTP)
# SENDGRID_API_KEY=your-sendgrid-api-key

# Email sender details
EMAIL_FROM_ADDRESS=noreply@chrona.com
EMAIL_FROM_NAME=Chrona - Time Tracking

# OTP email settings
OTP_SUBJECT=Votre code de vérification Chrona
OTP_EXPIRY_MINUTES=10

# Development/Testing settings
EMAIL_TEST_MODE=false
# EMAIL_TEST_RECIPIENT=test@example.com  # Override recipient in test mode
