# npm configuration for Chrona mobile app

# Continue installation even if optional dependencies fail
optional=true

# Strict peer dependencies (fail on peer dep conflicts)
legacy-peer-deps=false

# Save exact versions in package.json (not ranges)
save-exact=false
